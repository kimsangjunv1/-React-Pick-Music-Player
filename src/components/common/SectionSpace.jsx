import React from 'react'

const SectionSpace = ({ space }) => {
  return (
    <div className="space" style={{ margin: `${space}px 0` }}>
      {/* SectionSpace */}
    </div>
  )
}

export default SectionSpace
