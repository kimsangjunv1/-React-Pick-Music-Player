import React from 'react'

const SectionSpace = ({ space }) => {
  return (
    <div
      className="space"
      style={{
        margin: `${space}px 0`,
        margin: '35px 0px',
        height: '1px',
        width: '100%',
      }}
    >
      {/* SectionSpace */}
    </div>
  )
}

export default SectionSpace
